# Learning Page - Custom Translations ✅

## Реализовано

Страница обучения (LearningPage) теперь использует **кастомные переводы** вместо автоматического перевода Weglot.

### Как это работает:

1. **Weglot исключён** - вся страница обучения имеет класс `wg-notranslate` и атрибут `data-wg-notranslate`
2. **Определение языка** - используется хук `useWeglot()` для определения текущего языка Weglot
3. **Двуязычный контент** - все уроки хранятся в двух версиях (ru и en) в файле `src/data/learningLessons.ts`
4. **Автоматическое переключение** - при смене языка в Weglot контент автоматически переключается

## Структура файлов:

### src/data/learningLessons.ts
```typescript
export interface Lesson {
  id: number;
  title: {
    ru: string;
    en: string;
  };
  content: {
    ru: string;
    en: string;
  };
}

export const lessons: Lesson[] = [
  {
    id: 1,
    title: {
      ru: 'Старт для фрилансеров',
      en: 'Getting Started as a Freelancer'
    },
    content: {
      ru: '...',
      en: '...'
    }
  },
  // ... остальные уроки
];
```

### src/pages/LearningPage.tsx
```typescript
import { useWeglot } from '@/hooks/useWeglot';
import { lessons } from '@/data/learningLessons';

export default function LearningPage() {
  const { currentLang } = useWeglot();

  // Определяем язык (ru или en)
  const lang = currentLang === 'en' ? 'en' : 'ru';

  // Получаем переведённый контент
  const lessonTitle = currentLesson.title[lang];
  const lessonContent = currentLesson.content[lang];

  return (
    <div className="wg-notranslate" data-wg-notranslate>
      {/* Весь контент использует переменную lang */}
      <h1>{lang === 'en' ? 'Learning' : 'Обучение'}</h1>
      <h2>{lessonTitle}</h2>
      <div>{lessonContent}</div>
    </div>
  );
}
```

## Переведённые элементы:

### Статические тексты UI:
- ✅ "Обучение" → "Learning"
- ✅ "Пройди уроки..." → "Complete the lessons..."
- ✅ "Уроки" → "Lessons"
- ✅ "Урок X из Y" → "Lesson X of Y"
- ✅ "Пройдено: X из Y" → "Completed: X of Y"
- ✅ "Назад" → "Back"
- ✅ "Далее" → "Next"
- ✅ "На главную" → "Home"
- ✅ "Главная мысль" → "Key Takeaway"

### Контент уроков:
- ✅ Урок 1: "Старт для фрилансеров" → "Getting Started as a Freelancer"
- ✅ Урок 2: "Как получить первый заказ" → "How to Get Your First Order"
- ✅ Урок 3: "Как общаться с клиентами" → "How to Communicate with Clients"
- ✅ Полный текст всех уроков на двух языках

## Поддержка форматирования:

Парсер текста поддерживает оба языка:
- Заголовки с номерами: `1. Название` / `1. Title`
- Ключевые мысли: `Главная мысль:` / `Key takeaway:`
- Примеры: `Примеры:` / `Examples:`
- Кавычки: `«...»` (ru) и `"..."` (en)

## Как добавить новые уроки:

1. Откройте `src/data/learningLessons.ts`
2. Добавьте новый объект в массив `lessons`:

```typescript
{
  id: 4,
  title: {
    ru: 'Название урока на русском',
    en: 'Lesson Title in English'
  },
  content: {
    ru: 'Текст урока на русском...',
    en: 'Lesson text in English...'
  }
}
```

3. Сохраните файл - всё готово!

## Тестирование:

1. Запустите проект: `npm run dev`
2. Откройте страницу "Обучение"
3. Переключите язык через Weglot:
   - При выборе "English" → контент на английском
   - При выборе любого другого языка → контент на русском
4. Убедитесь, что:
   - ✅ Заголовки переведены
   - ✅ Кнопки переведены
   - ✅ Текст уроков переведён
   - ✅ Форматирование сохранено
   - ✅ Навигация работает

## Преимущества:

✅ **Контроль качества** - переводы делаются вручную, а не автоматически
✅ **Экономия слов Weglot** - страница обучения не считается в лимит
✅ **Мгновенное переключение** - не нужно ждать загрузки переводов
✅ **Поддержка форматирования** - сохраняется структура и стиль текста
✅ **Легко добавлять новые языки** - просто добавьте новое поле в объект

## Готово! 🎉

Страница обучения полностью переведена и работает с определением языка Weglot.
